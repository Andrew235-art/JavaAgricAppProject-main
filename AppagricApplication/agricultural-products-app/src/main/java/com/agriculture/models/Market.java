public class Market {
    private String name;
    private String type;
    private double yield;

    // Constructor, Getters, and Setters
    public Crop(String name, String type, double yield) {
        this.name = name;
        this.type = type;
        this.yield = yield;
    }

    // Getters and Setters
}